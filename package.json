{
  "name": "agent-embed-monorepo",
  "private": true,
  "scripts": {
    "clean": "rm -rf js/dist && rm -rf react/dist && rm -rf nextjs/dist",
    "build": "npm run clean && cd js && npm run build && cd ../react && npm run build && cd ../nextjs && npm run build",
    "test": "echo 'Add test scripts here'"
  },
  "dependencies": {
    "@agent-embed/nextjs": "file:../nextjs",
    "@agent-embed/react": "file:../react",
    "@agent-embed/js": "file:../js"
 }
}

